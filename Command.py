from enum import Enum

# for enum34, or the stdlib version
# from aenum import Enum  # for the aenum version
class Command(Enum):
    PLACE=1
    MOVE=2
    LEFT=3
    RIGHT=4
    REPORT=5



#command = Enum#
