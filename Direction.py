from enum import Enum

class Direction(Enum):
	NORTH = 0
	EAST = 1
	SOUTH = 2
	WEST = 3
"""
directionDict={0:'NORTH',1:'EAST',2:'SOUTH',3:'WEST'}
#directionDict.update({1:'adsfa'})
print(directionDict)
def __init__(self,directionDict):
	self.directionDict=directionDict
	#def rotate(self,step):
#	if self.directionIndex+step<0
 	print(directionDict)

def rotate(self,step) {
newIndex = (self.Direction.value + step) < 0 ?
map.size() - 1:
(this.directionIndex + step) % map.size();

return Direction.valueOf(newIndex);
}

"""